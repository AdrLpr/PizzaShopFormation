{% extends "front/base.html.twig" %}

{% block sub_title %}Pizza{% endblock %}

{% block content %}
    {% for pizza in pizzas %}
        <div>
            <h2> {{pizza.name}} </h2>
            <h3> {{pizza.price}} €</h3>
            <h4> 
            <ul> {% for ingredient in pizza.Ingredient %}
                    <li> {{ingredient.name}} </li>
                
            {% endfor %}
        </div>
    {% endfor %}
{% endblock %}